# 48 — Optimize the dominant measured seam

**What to build:** Implement one context-sized optimization against the single highest-impact actionable bottleneck identified by the accepted profiles, preserving all semantic and operational invariants.

**Blocked by:** 47 — Profile the engine and set measured defaults

**Status:** ready-for-agent

- [ ] The ticket identifies the exact profile evidence and expected impact before changing implementation.
- [ ] The optimization stays behind private seams unless evidence requires a separately reviewed public change.
- [ ] Reference, audit, structured/encoded parity, and operational suites remain green.
- [ ] Before-and-after measurements use the same reproducible workload and report allocation, latency, CPU, memory, and useful throughput.
- [ ] No unrelated secondary bottleneck is absorbed into this ticket.

