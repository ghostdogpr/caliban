# 01 — Create the fresh gateway foundation

**What to build:** Create the unpublished embedded gateway module and its minimal immutable public description/runtime shell, without preserving compatibility with the abandoned prototype. Record the approved sequencing deviation: structured embedded execution is delivered before Quick and encoded integration, while response projection is designed for multiple sinks from its first implementation.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] The gateway module compiles on the provisional supported JVM Scala matrix and publication remains disabled.
- [ ] Public descriptions have private representations, preserve contravariant environment requirements, and expose none of the abandoned prototype API.
- [ ] Gateway internals remain private and dependencies point from gateway to existing Caliban modules, never from core to gateway.
- [ ] The sequencing deviation and the sink-parameterized projection guardrail are recorded before implementation proceeds.

